# _working

`_working/` is the active sandbox for the current iteration.

Tracked files in this directory:

- `ITERATION.md` — current iteration descriptor and phase context.
- `README.md` — this policy note.

All other temporary files in `_working/` should be treated as disposable local scratch material and should not be committed unless explicitly approved.

Durable product truth belongs in Canon docs, decision records, issues, or PRs — not in `_working/`.
